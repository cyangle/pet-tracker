{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build pet-tracker release",
			"type": "shell",
			"options": {
				"cwd": "${workspaceRoot}/"
			},
			"command": "docker-compose build"
		},
		{
			"label": "amber watch",
			"type": "shell",
			"options": {
				"cwd": "${workspaceRoot}/"
			},
			"command": "amber watch",
			"problemMatcher": []
		}
	]
}
